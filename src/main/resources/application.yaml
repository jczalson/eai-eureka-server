spring:
  application:
    name: eureka-server
server:
  port: 8761    
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    renewal-percent-threshold: 0.1  

#try it